import styled from 'styled-components';

export const Title = styled.h3 `
    color: ${({ theme }) => theme.text};
    text-align: center;
    font-size: 1.5rem;
    padding-bottom: 10px;
    position: relative;
    z-index: 0;
`